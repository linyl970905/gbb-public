<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tencent.wxcloudrun.dao.PunchAttendMapper">

    <select id="getRuleByMerId" parameterType="java.lang.Integer" resultType="com.tencent.wxcloudrun.model.PunchAttendRule">
        SELECT * FROM punch_attend_rule WHERE mer_id = #{merId}
    </select>

    <insert id="addRule" parameterType="com.tencent.wxcloudrun.model.PunchAttendRule">
        INSERT INTO punch_attend_rule
            (mer_id, punch_type, one_start_time, one_end_time, two_start_time, two_end_time,
             three_start_time, three_end_time, four_start_time, four_end_time, five_start_time,
             five_end_time, six_start_time, six_end_time, create_time)
        VALUES
            (#{merId}, #{punchType}, #{oneStartTime}, #{oneEndTime}, #{twoStartTime}, #{twoEndTime},
             #{threeStartTime}, #{threeEndTime}, #{fourStartTime}, #{fourEndTime}, #{fiveStartTime},
             #{fiveEndTime}, #{sixStartTime}, #{sixEndTime}, NOW())
    </insert>
</mapper>
